

void main(){
  print("Arithmetic Operators");

  const int one = 1;
  const int two = 2;
  int three = one + two;

  // Notes: 
  //  using "/" (divided by) results always a double
  //  using operators as arithmetic to calculate is first evaluate then assigned
  //  % remainder of number 
  //  using "~/" (divided by) results always in int
  //  dart support prefix and postfix ++ and -- as ++myVar or myVar--
  //  other operators: + and - and * and negative of number ( -myVar)
}