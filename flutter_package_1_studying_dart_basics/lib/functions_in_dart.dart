

void main(){

  /**
   * - dart is true object-oriented, so even functions have a type Function
   * 
   * 
   *  bool isNoble(int atomicNumber) {
        return _nobleGases[atomicNumber] != null;
      } 

      or

      bool isNoble(int atomicNumber) => _nobleGases[atomicNumber] != null;
      (shorthand of { return expr; }.
      )      
   *  /// Sets the [bold] and [hidden] flags ...
      void enableFlags({bool bold = false, bool hidden = false}) {...}

      - if you use like this enableFlags(bold: true);, hidden will be false by default

      const Scrollbar({super.key, required Widget child}); if you want to make a parameter required

      - if you want to make the parameters NAMED, you can use:

      enableFlags({bool bold = false, bool hidden = false}) {...} the {} in parameters
   * 
   * 
   * 
   * 
   */
}